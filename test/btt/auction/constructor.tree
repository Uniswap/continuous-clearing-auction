ConstructorTest
├── when claimBlock LT endBlock
│   └── it reverts with {ClaimBlockIsBeforeEndBlock}
├── when total supply LTE MAX_TOTAL_SUPPLY
│   └──  it calculates max bid price to be within uni v4 liquidity bounds
├── when type(uint256).max / totalSupply < uni v4 max tick
│   └── it sets bid max price to be ConstantsLib.MAX_BID_PRICE / totalSupply
└── when claimBlock GE endBlock
    ├── when uint256 max div totalSupply GE uni v4 max tick
    │   ├── it writes CLAIM_BLOCK
    │   ├── it writes VALIDATION_HOOK
    │   ├── it writes BID_MAX_PRICE as v4 max tick
    |   └── when floor price GT v4 max tick
    |       └── it reverts with {FloorPriceAndTickSpacingGreaterThanMaxBidPrice}
    └── when uint256 max div totalSupply LE uni v4 max tick
        ├── it writes CLAIM_BLOCK
        ├── it writes VALIDATION_HOOK
        ├── it writes BID_MAX_PRICE as type(uint256).max / totalSupply
        └── when floor price GT type(uint256).max / totalSupply
            └── it reverts with {FloorPriceAndTickSpacingGreaterThanMaxBidPrice}